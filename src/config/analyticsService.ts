// Analytics service config — separate from main backend.
// Mirrors the pattern in emailService.ts.

const ANALYTICS_CONFIG = {
  BASE_URL: 'https://stats.collabscafe.com',
  API_KEY: '123456',
  // BASE_URL: 'http://localhost:5000',
  // API_KEY: 'cc-analytics-admin-2026',
};

export default ANALYTICS_CONFIG;
